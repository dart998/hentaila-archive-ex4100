package web

import (
	"context"
	"crypto/aes"
	"crypto/cipher"
	"encoding/base64"
	"encoding/json"
	"errors"
	"fmt"
	"io"
	"log"
	"net/http"
	"net/url"
	"os"
	"path/filepath"
	"regexp"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/dart998/hentaila-archive-ex4100/internal/hentaila"
	libraryindex "github.com/dart998/hentaila-archive-ex4100/internal/library"
)

type downloadEpisodeState struct {
	Episode int `json:"episode"`; Status string `json:"status"`; Provider string `json:"provider,omitempty"`; File string `json:"file,omitempty"`; Bytes int64 `json:"bytes,omitempty"`; TotalBytes int64 `json:"total_bytes,omitempty"`; Error string `json:"error,omitempty"`
}
type seriesDownloadState struct {
	Slug string `json:"slug"`; Running bool `json:"running"`; Cancelled bool `json:"cancelled,omitempty"`; Current int `json:"current"`; Total int `json:"total"`; Downloaded int `json:"downloaded"`; Skipped int `json:"skipped"`; Errors int `json:"errors"`; CurrentFile string `json:"current_file,omitempty"`; LastError string `json:"last_error,omitempty"`; Started string `json:"started,omitempty"`; Finished string `json:"finished,omitempty"`; Episodes []downloadEpisodeState `json:"episodes"`; ForceUnplayable bool `json:"-"`
}
type episodeNotification struct { ID string `json:"id"`; Slug string `json:"slug"`; Title string `json:"title"`; Episode int `json:"episode"`; CreatedAt string `json:"created_at"`; ReadAt string `json:"read_at,omitempty"` }

var downloadStates=struct{sync.RWMutex;m map[string]*seriesDownloadState}{m:map[string]*seriesDownloadState{}}
var downloadCancels=struct{sync.Mutex;m map[string]context.CancelFunc}{m:map[string]context.CancelFunc{}}
var downloadQueueMu sync.Mutex
var megaDownloadRE=regexp.MustCompile(`server:"Mega",url:"([^"]+)"`)
var errDownloadSeriesNotFound=errors.New("serie no encontrada")
var errDownloadSeriesWithoutEpisodes=errors.New("sin episodios")

func (s *Server) persistDownloadState(st *seriesDownloadState){b,_:=json.Marshal(st);_=s.db.SetSetting("download_state_"+st.Slug,string(b))}
func (s *Server) setDL(st *seriesDownloadState,f func(*seriesDownloadState)){downloadStates.Lock();f(st);cp:=*st;cp.Episodes=append([]downloadEpisodeState(nil),st.Episodes...);downloadStates.Unlock();s.persistDownloadState(&cp)}
func episodeState(st *seriesDownloadState,ep int)*downloadEpisodeState{for i:=range st.Episodes{if st.Episodes[i].Episode==ep{return &st.Episodes[i]}};return nil}

func (s *Server) downloadItem(ctx context.Context,slug string)(hentaila.Item,int,error){item,ok:=s.itemForSlug(slug);total:=0;if ok{total=item.Total;if total<1{total=s.db.SeriesEpisodeCount(slug)};if total>0{return item,total,nil}};if e:=s.crawl.RunTarget(ctx,slug);e!=nil{if strings.Contains(e.Error(),"404"){return hentaila.Item{},0,errDownloadSeriesNotFound};return hentaila.Item{},0,fmt.Errorf("no se pudo indexar la serie desde HentaiLA: %w",e)};item,ok=s.itemForSlug(slug);if !ok{return hentaila.Item{},0,errDownloadSeriesNotFound};total=item.Total;if total<1{total=s.db.SeriesEpisodeCount(slug)};if total<1{return item,0,errDownloadSeriesWithoutEpisodes};return item,total,nil}

func (s *Server) downloadSeriesAPI(w http.ResponseWriter,r *http.Request){
	if r.Method!=http.MethodPost{http.Error(w,"method not allowed",405);return};if e:=r.ParseForm();e!=nil{http.Error(w,e.Error(),400);return};slug:=strings.TrimSpace(r.FormValue("slug"));if slug==""{http.Error(w,"serie no encontrada",404);return}
	if r.FormValue("action")=="cancel"{s.cancelSeriesDownload(w,slug);return}
	item,total,e:=s.downloadItem(r.Context(),slug);if e!=nil{switch{case errors.Is(e,errDownloadSeriesNotFound):http.Error(w,"serie no encontrada",404);case errors.Is(e,errDownloadSeriesWithoutEpisodes):http.Error(w,"sin episodios",409);default:http.Error(w,e.Error(),502)};return}
	if lib,e:=libraryindex.Scan(s.libraryRoot);e==nil{_=s.db.ReplaceLibrary(lib)};force:=r.FormValue("force_unplayable")=="1";if !force{var bad []int;for ep:=1;ep<=total;ep++{if info,e:=s.localEpisode(slug,ep);e==nil&&info.Available&&!info.BrowserPlayable{bad=append(bad,ep)}};if len(bad)>0{w.Header().Set("Content-Type","application/json");w.WriteHeader(http.StatusConflict);_=json.NewEncoder(w).Encode(map[string]any{"requires_confirmation":true,"unplayable_episodes":bad});return}}
	downloadStates.Lock();if x:=downloadStates.m[slug];x!=nil&&x.Running{downloadStates.Unlock();http.Error(w,"descarga en curso",409);return};st:=&seriesDownloadState{Slug:slug,Running:true,Total:total,Started:time.Now().Format(time.RFC3339),Episodes:make([]downloadEpisodeState,total),ForceUnplayable:force};for i:=1;i<=total;i++{st.Episodes[i-1]=downloadEpisodeState{Episode:i,Status:"pending"}};downloadStates.m[slug]=st;downloadStates.Unlock()
	ctx,cancel:=context.WithCancel(context.Background());downloadCancels.Lock();downloadCancels.m[slug]=cancel;downloadCancels.Unlock();s.persistDownloadState(st);go s.runMegaSeries(ctx,item,st);w.Header().Set("Content-Type","application/json");w.WriteHeader(http.StatusAccepted);_=json.NewEncoder(w).Encode(st)
}
func (s *Server) cancelSeriesDownload(w http.ResponseWriter,slug string){downloadStates.RLock();st:=downloadStates.m[slug];running:=st!=nil&&st.Running;downloadStates.RUnlock();if !running{http.Error(w,"no hay descarga en curso",409);return};downloadCancels.Lock();cancel:=downloadCancels.m[slug];downloadCancels.Unlock();if cancel!=nil{cancel()};s.setDL(st,func(x *seriesDownloadState){x.Cancelled=true});w.Header().Set("Content-Type","application/json");w.WriteHeader(http.StatusAccepted);_=json.NewEncoder(w).Encode(map[string]bool{"cancelling":true})}
func (s *Server) finishCancelled(st *seriesDownloadState){s.setDL(st,func(x *seriesDownloadState){x.Running=false;x.Cancelled=true;x.CurrentFile="";x.LastError="";x.Finished=time.Now().Format(time.RFC3339);if q:=episodeState(x,x.Current);q!=nil&&(q.Status=="checking"||q.Status=="downloading"){q.Status="cancelled";q.Error=""}})}

func (s *Server) downloadStatusAPI(w http.ResponseWriter,r *http.Request){if r.URL.Query().Get("notifications")=="1"{s.notificationsAPI(w,r);return};if r.Method!=http.MethodGet{http.Error(w,"method not allowed",405);return};slug:=strings.TrimSpace(r.URL.Query().Get("slug"));downloadStates.RLock();x:=downloadStates.m[slug];if x!=nil{cp:=*x;cp.Episodes=append([]downloadEpisodeState(nil),x.Episodes...);downloadStates.RUnlock();if !cp.Running{cp.Finished=""};w.Header().Set("Content-Type","application/json");_=json.NewEncoder(w).Encode(cp);return};downloadStates.RUnlock();raw:=strings.TrimSpace(s.db.GetSetting("download_state_"+slug));if raw==""{http.Error(w,"sin descarga",404);return};var cp seriesDownloadState;if json.Unmarshal([]byte(raw),&cp)!=nil{http.Error(w,"sin descarga",404);return};if cp.Running{cp.Running=false;cp.LastError="Descarga interrumpida por reinicio del contenedor";cp.Finished=time.Now().Format(time.RFC3339);s.persistDownloadState(&cp)};if !cp.Running{cp.Finished=""};w.Header().Set("Content-Type","application/json");_=json.NewEncoder(w).Encode(cp)}

func (s *Server) loadNotifications()[]episodeNotification{var out []episodeNotification;_=json.Unmarshal([]byte(s.db.GetSetting("episode_notifications_json")),&out);cut:=time.Now().Add(-24*time.Hour);keep:=out[:0];changed:=false;for _,n:=range out{if n.ReadAt!=""{if t,e:=time.Parse(time.RFC3339,n.ReadAt);e==nil&&t.Before(cut){changed=true;continue}};keep=append(keep,n)};if changed{b,_:=json.Marshal(keep);_=s.db.SetSetting("episode_notifications_json",string(b))};return keep}
func (s *Server) notificationsAPI(w http.ResponseWriter,r *http.Request){if r.Method==http.MethodGet{n:=s.loadNotifications();unread:=0;for _,x:=range n{if x.ReadAt==""{unread++}};w.Header().Set("Content-Type","application/json");_=json.NewEncoder(w).Encode(map[string]any{"notifications":n,"unread":unread});return};if r.Method!=http.MethodPost{http.Error(w,"method not allowed",405);return};if e:=r.ParseForm();e!=nil{http.Error(w,e.Error(),400);return};action,id:=r.FormValue("action"),r.FormValue("id");n:=s.loadNotifications();now:=time.Now().Format(time.RFC3339);for i:=range n{if action=="read_all"||(action=="read"&&n[i].ID==id){if n[i].ReadAt==""{n[i].ReadAt=now}}};b,_:=json.Marshal(n);_=s.db.SetSetting("episode_notifications_json",string(b));w.Header().Set("Content-Type","application/json");_=json.NewEncoder(w).Encode(map[string]bool{"ok":true})}

func redactMegaURL(raw string)string{s:=strings.TrimSpace(raw);lower:=strings.ToLower(s);type marker struct{idx,n int};best:=marker{idx:-1};for _,sep:=range []string{"#","%23",`\u0023`}{i:=strings.Index(lower,strings.ToLower(sep));if i>=0&&(best.idx<0||i<best.idx){best=marker{idx:i,n:len(sep)}}};if best.idx<0{return s};start:=best.idx+best.n;if start>len(s){return s};return s[:start]+fmt.Sprintf("<redacted:%d>",len(s)-start)}
func logMegaParse(stage,raw string){u,e:=url.Parse(raw);if e!=nil{log.Printf("[MEGA DEBUG] stage=%s raw=%q parse_error=%v",stage,redactMegaURL(raw),e);return};log.Printf("[MEGA DEBUG] stage=%s raw=%q host=%q path=%q raw_path=%q fragment_len=%d has_pct23=%t has_u0023=%t has_escaped_slash=%t",stage,redactMegaURL(raw),u.Hostname(),u.Path,u.RawPath,len(u.Fragment),strings.Contains(strings.ToLower(raw),"%23"),strings.Contains(strings.ToLower(raw),`\u0023`),strings.Contains(raw,`\/`))}

func (s *Server) runMegaSeries(ctx context.Context,item hentaila.Item,st *seriesDownloadState){downloadQueueMu.Lock();defer downloadQueueMu.Unlock();defer func(){downloadCancels.Lock();delete(downloadCancels.m,item.Slug);downloadCancels.Unlock()}();if ctx.Err()!=nil{s.finishCancelled(st);return};target,e:=s.downloadTargetDir(item);if e!=nil{s.setDL(st,func(x *seriesDownloadState){x.Running=false;x.Errors++;x.LastError=e.Error();x.Finished=time.Now().Format(time.RFC3339)});return};for ep:=1;ep<=st.Total;ep++{if ctx.Err()!=nil{s.finishCancelled(st);return};s.setDL(st,func(x *seriesDownloadState){x.Current=ep;x.CurrentFile="";if q:=episodeState(x,ep);q!=nil{q.Status="checking"}});if info,e:=s.localEpisode(item.Slug,ep);e==nil&&info.Available&&(info.BrowserPlayable||!st.ForceUnplayable){s.setDL(st,func(x *seriesDownloadState){x.Skipped++;if q:=episodeState(x,ep);q!=nil{q.Status="existing";q.File=info.FileName}});continue};mega,e:=s.episodeMegaDownloadURL(ctx,item.Slug,ep);if e!=nil{if errors.Is(e,context.Canceled){s.finishCancelled(st);return};s.setDL(st,func(x *seriesDownloadState){x.Errors++;x.LastError=fmt.Sprintf("Ep %d: %v",ep,e);if q:=episodeState(x,ep);q!=nil{q.Status="error";q.Error=e.Error()}});continue};s.setDL(st,func(x *seriesDownloadState){if q:=episodeState(x,ep);q!=nil{q.Status="downloading";q.Provider="Mega"}});p,e:=downloadMega(ctx,mega,target,func(name string,done,total int64){s.setDL(st,func(x *seriesDownloadState){x.CurrentFile=name;if q:=episodeState(x,ep);q!=nil{q.File=name;q.Bytes=done;q.TotalBytes=total}})});if e!=nil{if errors.Is(e,context.Canceled){s.finishCancelled(st);return};s.setDL(st,func(x *seriesDownloadState){x.Errors++;x.LastError=fmt.Sprintf("Ep %d: %v",ep,e);if q:=episodeState(x,ep);q!=nil{q.Status="error";q.Error=e.Error()}});continue};p=ensureDownloadedEpisodeName(p,item.MediaID,ep);if items,e:=libraryindex.Scan(s.libraryRoot);e==nil{_=s.db.ReplaceLibrary(items)};s.setDL(st,func(x *seriesDownloadState){x.Downloaded++;x.CurrentFile=filepath.Base(p);if q:=episodeState(x,ep);q!=nil{q.Status="completed";q.File=filepath.Base(p);q.Error=""}})};s.setDL(st,func(x *seriesDownloadState){x.Running=false;x.CurrentFile="";x.Finished=time.Now().Format(time.RFC3339)})}

func ensureDownloadedEpisodeName(path string,mediaID hentaila.IDString,episode int)string{if episodeFileRank(filepath.Base(path),mediaID,episode)<99{return path};dir,name:=filepath.Dir(path),filepath.Base(path);prefix:=fmt.Sprintf("EP%02d_",episode);if id:=strings.TrimSpace(string(mediaID));id!=""{prefix=id+"_"+strconv.Itoa(episode)+"_"};dest:=filepath.Join(dir,prefix+name);if _,e:=os.Stat(dest);e==nil{ext:=filepath.Ext(name);base:=strings.TrimSuffix(prefix+name,ext);for i:=1;;i++{candidate:=filepath.Join(dir,fmt.Sprintf("%s_%d%s",base,i,ext));if _,e:=os.Stat(candidate);os.IsNotExist(e){dest=candidate;break}}};if e:=os.Rename(path,dest);e==nil{_=os.Chmod(dest,0666);return dest};return path}
func (s *Server) episodeMegaDownloadURL(ctx context.Context,slug string,ep int)(string,error){b,e:=s.fetchEpisodeHTML(ctx,slug,ep);if e!=nil{return "",e};matches:=megaDownloadRE.FindAllSubmatch(b,-1);log.Printf("[MEGA DEBUG] slug=%s episode=%d candidates=%d",slug,ep,len(matches));for i,m:=range matches{if len(m)<2{continue};raw:=string(m[1]);logMegaParse(fmt.Sprintf("extract-%d",i+1),raw);u,e:=url.Parse(raw);if e!=nil{continue};host,path:=strings.ToLower(u.Hostname()),strings.ToLower(u.Path);if (strings.Contains(host,"mega.nz")||strings.Contains(host,"mega.co.nz"))&&(strings.Contains(path,"/file/")||strings.HasPrefix(u.Fragment,"!")){log.Printf("[MEGA DEBUG] slug=%s episode=%d selected=%d",slug,ep,i+1);return raw,nil}};log.Printf("[MEGA DEBUG] slug=%s episode=%d no_download_candidate",slug,ep);return "",errors.New("sin enlace de descarga Mega para este episodio")}
func (s *Server) downloadTargetDir(item hentaila.Item)(string,error){lib,e:=libraryindex.Scan(s.libraryRoot);if e!=nil{return "",e};_=s.db.ReplaceLibrary(lib);if c:=localFolderCandidates(item,lib);len(c)>0{return c[0].Item.Path,nil};name:=safeDirName(item.Title);if name==""{name="HentaiLA"};dir:=filepath.Join(s.libraryRoot,name);if e=os.MkdirAll(dir,0777);e!=nil{return "",e};if e=os.Chmod(dir,0777);e!=nil{return "",e};return dir,nil}
func safeDirName(s string)string{return strings.TrimSpace(strings.NewReplacer("/","-","\\","-",":"," -","*","","?","","\"","","<","",">","","|","-").Replace(s))}

type megaResp struct{G string `json:"g"`;S int64 `json:"s"`;AT string `json:"at"`}
type progressReader struct{r io.Reader;done,total int64;last time.Time;cb func(int64,int64)}
func (p *progressReader)Read(b []byte)(int,error){n,e:=p.r.Read(b);p.done+=int64(n);now:=time.Now();if p.cb!=nil&&(p.last.IsZero()||now.Sub(p.last)>=time.Second||p.done==p.total){p.last=now;p.cb(p.done,p.total)};return n,e}
func downloadMega(ctx context.Context,raw,target string,progress func(string,int64,int64))(string,error){logMegaParse("download",raw);u,e:=url.Parse(raw);if e!=nil{return "",e};parts:=strings.Split(strings.Trim(u.Path,"/"),"/");var handle,fragment string;if len(parts)>=2&&strings.EqualFold(parts[0],"file"){handle,fragment=parts[1],u.Fragment}else if u.Fragment!=""&&strings.Contains(u.Fragment,"!"){bits:=strings.Split(strings.TrimPrefix(u.Fragment,"!"),"!");if len(bits)>=2{handle,fragment=bits[0],bits[1]}};log.Printf("[MEGA DEBUG] stage=download parts=%d handle_len=%d fragment_len=%d",len(parts),len(handle),len(fragment));if handle==""||fragment==""{log.Printf("[MEGA DEBUG] stage=download invalid_url handle_empty=%t fragment_empty=%t",handle=="",fragment=="");return "",errors.New("URL Mega de descarga invalida")};keyRaw,e:=base64.RawURLEncoding.DecodeString(strings.TrimSpace(fragment));if e!=nil||len(keyRaw)!=32{log.Printf("[MEGA DEBUG] stage=download invalid_key decoded_len=%d decode_error=%v",len(keyRaw),e);return "",errors.New("clave Mega invalida")};key:=make([]byte,16);for i:=0;i<16;i++{key[i]=keyRaw[i]^keyRaw[i+16]};iv:=make([]byte,16);copy(iv,keyRaw[16:24]);payload:=fmt.Sprintf(`[{"a":"g","g":1,"p":"%s"}]`,handle);api:="https://g.api.mega.co.nz/cs?id="+strconv.FormatInt(time.Now().UnixNano(),10);req,e:=http.NewRequestWithContext(ctx,http.MethodPost,api,strings.NewReader(payload));if e!=nil{return "",e};req.Header.Set("Content-Type","application/json");resp,e:=http.DefaultClient.Do(req);if e!=nil{return "",e};b,e:=io.ReadAll(io.LimitReader(resp.Body,2<<20));resp.Body.Close();if e!=nil{return "",e};var rr []megaResp;if json.Unmarshal(b,&rr)!=nil||len(rr)==0||rr[0].G==""{return "",fmt.Errorf("Mega API: %s",strings.TrimSpace(string(b)))};name:="mega-"+handle+".mp4";if rr[0].AT!=""{if n:=megaName(rr[0].AT,key);n!=""{name=filepath.Base(n)}};if e=os.MkdirAll(target,0777);e!=nil{return "",e};dest:=filepath.Join(target,name);if _,e:=os.Stat(dest);e==nil{ext:=filepath.Ext(name);base:=strings.TrimSuffix(name,ext);for i:=1;;i++{candidate:=filepath.Join(target,fmt.Sprintf("%s (descarga %d)%s",base,i,ext));if _,e:=os.Stat(candidate);os.IsNotExist(e){dest=candidate;name=filepath.Base(candidate);break}}};part:=dest+".part";cleanup:=true;defer func(){if cleanup{_=os.Remove(part)}}();get,e:=http.NewRequestWithContext(ctx,http.MethodGet,rr[0].G,nil);if e!=nil{return "",e};r,e:=http.DefaultClient.Do(get);if e!=nil{return "",e};defer r.Body.Close();if r.StatusCode<200||r.StatusCode>=300{return "",fmt.Errorf("Mega descarga: %s",r.Status)};f,e:=os.OpenFile(part,os.O_CREATE|os.O_TRUNC|os.O_WRONLY,0666);if e!=nil{return "",e};_=os.Chmod(part,0666);block,e:=aes.NewCipher(key);if e!=nil{f.Close();return "",e};stream:=&cipher.StreamReader{S:cipher.NewCTR(block,iv),R:r.Body};pr:=&progressReader{r:stream,total:rr[0].S,cb:func(done,total int64){if progress!=nil{progress(name,done,total)}}};if progress!=nil{progress(name,0,rr[0].S)};_,copyErr:=io.Copy(f,pr);closeErr:=f.Close();if copyErr!=nil{return "",copyErr};if closeErr!=nil{return "",closeErr};if ctx.Err()!=nil{return "",ctx.Err()};if rr[0].S>0{st,e:=os.Stat(part);if e!=nil{return "",e};if st.Size()!=rr[0].S{return "",fmt.Errorf("descarga Mega incompleta: %d/%d bytes",st.Size(),rr[0].S)}};if e=os.Rename(part,dest);e!=nil{return "",e};cleanup=false;_=os.Chmod(dest,0666);return dest,nil}
func megaName(at string,key []byte)string{b,e:=base64.RawURLEncoding.DecodeString(at);if e!=nil||len(b)==0||len(b)%16!=0{return ""};block,e:=aes.NewCipher(key);if e!=nil{return ""};out:=make([]byte,len(b));cipher.NewCBCDecrypter(block,make([]byte,16)).CryptBlocks(out,b);s:=strings.TrimRight(string(out),"\x00");if !strings.HasPrefix(s,"MEGA"){return ""};var x struct{N string `json:"n"`};if json.Unmarshal([]byte(s[4:]),&x)!=nil{return ""};return x.N}
